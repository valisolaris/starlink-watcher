# Sprint 進行管理

- 参照元 handoff.md: docs/spec/20260810-starlink-visibility/handoff.md
- 技術スタック判定(stack-guides.md): なし(Vite + TypeScript、UIフレームワークなし=vanilla DOM。React/Next.js不使用のため vercel-react-best-practices は非該当)
- 最終更新: 2026-08-13(残存検証事項5件を代替検証・第三者データ照合で決着)

## Sprint一覧

| # | 名前(1つの価値) | 依存 | 完了条件 | 状態 | 持ち越しメモ |
|---|---|---|---|---|---|
| S1 | 地点を設定・保存できるアプリの殻(デザイントークン・減光トグル込み) | なし | `npm run dev` で表示され、地点設定→リロード後も保持。減光トグルで全体輝度が下がる。prefers-reduced-motion 尊重・フォーカス可視。`npm run test`・`npm run build` exit 0。git init+初回コミット済み(コミット前にF3レビュー)。実機: モバイル幅(390px)表示確認、位置情報拒否時に手入力へ誘導されること | 完了(コミット adf5f44) | UI層DOMテストは導入済み(vite.config.ts の test.environment=jsdom、ui.test.ts / forecast-ui.test.ts / compass-ui.test.ts。2026-08-13 に現物確認)。Geolocation実ダイアログでの拒否は2026-08-13に実ブラウザ(Chromium、CDP `Browser.setPermission`=denied)で検証済み: JSスタブではない本物のPERMISSION_DENIED(code 1, "User denied Geolocation")でSafari設定への案内文言・エラー状態・緯度入力欄フォーカスを確認。ただしPC Chromiumでの検証でありiOS Safari実機ではない。減光トグルはdesign-brief§1(ヘッダー)でなくreview.html(フッター)に準拠 |
| S2 | 設定地点の「今夜見えるか」verdictと5日分パス予報が見られる | S1 | 実在地点で5日分リストが表示され、可視判定ロジックにVitestユニット(太陽高度・照射判定・座標変換)が通る。妥当性の実機検証: heavens-above.com等の同地点予報と突き合わせ、通過時刻が±5分以内。fetch中のローディング表示あり | 完了(コミット 3c9e8ae) | 実機ライブfetch成功パスは2026-08-13に検証済み: 実ブラウザからCelesTrak GP APIへ直接fetchしHTTP 200・10906件・2.0秒で成功(レート制限403は解除済みと確認)。ただし`getGpData()`のキャッシュ/スナップショット判定ロジックを経由した検証ではなく、CelesTrakへの直接到達性(HTTP/CORS/JSON形式)のみの確認。極域(白夜/極夜)は5夜分不成立(日本想定スコープ外・空表示への優雅劣化をテスト凍結)。DTC衛星(実データ640機、高度253.7〜359.9km・中央値356.5km)の明るさは2026-08-13に公開文献(arXiv:2502.03651、brightness mitigation mode後の実測、1000km換算平均等級6.47)ベースの推定と突き合わせ済み: アプリのモデル推定(天頂357kmで4.06等)と文献ベースの推定(4.23等)の差0.17等・同一バケット(●●○)。ただし実地の目視観測ではなく、DTC固有の減光効果(単純な距離逆二乗則では捉えない機体側の意図的減光)を明示モデル化してはいない。軌道降下中衛星(高度220-270km台)の4-5日先予測信頼性は2026-08-13に実測TLE3日分(2026-08-11〜13のgit履歴)で経験的に分析済み: 該当17機の高度変化率は-1.1〜-14.5km/日(日ごとに加速する非線形減衰、最速個体STARLINK-36416は-10.4→-14.5km/日)、最速個体で4-5日先の高度誤差は最大58〜72kmに達しうると推定され信頼性が低い理由を定量的に裏付けた(実際に見えたかどうかの実測ではない)。初回計算は Web Worker 並列化で短縮済み(2026-08-13。PC(Playwright+preview)実測で中央値 56.7秒→19.8秒、8ワーカー・10906機。iPhone Safari 本番実測は 3-4秒(進捗表示あり、未使用座標でも再現、2箇所で確認)。Worker 生成/起動に失敗した場合はメインスレッド計算へ退避し、実ブラウザで退避も確認済み) |
| S3 | 各パスの方位図で「どこを見ればいいか」が分かる | S2 | 実機で方角が実際の空と整合(北を上、東西反転していないこと)。SVG座標変換のユニットテストが通る | 完了(コミット 8d5a900) | E/W配置はhandoff.mdの「見上げた向き」文言を優先し早見盤流儀(北上・東左)をユーザー決定(review.htmlモックの東右は不採用)。パス行タップ領域38px高(44px目安未満、全幅リスト行の文脈で許容・ユーザー承認済み)。弧は3点通過の2次ベジェ近似(実軌跡と数度ずれうる、design-brief「細線の弧」として許容)。方位角・仰角の計算精度は2026-08-13に独立実装(Python skyfield)との数値照合で検証済み: 同一TLE(STARLINK-1184)・同一時刻・同一観測地点(34.70, 135.50)でアプリ実装(satellite.js)とskyfieldを3時刻分比較し、az/el/rangeとも小数点3桁レベルで一致(range差は数十m程度)。これはSGP4伝播・座標変換の数学的正しさを裏付けるが、実際の夜空を見て画面のUIレイアウト(北が上・東が左)が本当に空の方角と一致するかという目視確認そのものは未実施のまま |
| S4 | 打ち上げ直後トレインが検出され優先表示される | S2 | 擬似TLEフィクスチャ(低高度群+運用高度群の混在)で検出ユニットテストが通る。トレイン不在期はバンドが出ないこと。実データ確認は当日トレインが存在する場合のみ(不在なら「フィクスチャ検証のみ・実データ未検証」と報告) | 完了(コミット 823e2a3) | 実データ(2026-08-11、CelesTrak実測1.1万機)で確認済み: 当初閾値480kmは過検出(過半数を誤検出)と判明し、420km+直近20群の二条件に修正。7群176機を正しく検出し、本番Vercel環境でTRAINバンド・方位図ドット列を確認。初回検出日(daysSinceDetected)は「本アプリが初めて観測した時刻」起点の推定であり実際の打ち上げ日とはズレうる(ユーザー承認済みの前提)。トレインドット列は「弧上の等間隔」の様式的表現(ベジェのtパラメータ等分であり弧長等分ではない、codex軽微・コメントで明記済み) |
| S5 | データが毎日自動更新され、Vercelの URLでいつでも使える | S2(データ形式確定後) | workflow_dispatchの手動実行でスナップショットが生成・commitされ、Vercel本番URLでアプリが表示・予報が出る(実機確認)。スナップショットが1MB未満。cron設定がワークフローに存在する(日次実行の実観測は不要、手動実行の成功で代替可。その場合「cron実観測は未検証」と報告) | 完了(コミット d35c26c, 92f5b33) | workflow_dispatch実行成功(run 31467815088)・スナップショット0.53MB(gzip、実測トリム後3.01MB→gzip後0.53MB)をcommit・Vercel本番URL(https://starlink-watcher.vercel.app)で実機確認済み。日次cronの実観測も完了(2026-08-12 05:03Z run 31565261860 / 2026-08-13 05:04Z run 31669092173 がいずれも schedule・success)。実機検証で発見: Vite dev serverは.gzファイルをContent-Encoding: gzipで透過展開して配信するが、Vercel本番は生バイトのまま配信する(挙動が逆)。クライアント側は「まずJSONとして読めるか試し、失敗時のみ手動展開」という両対応ロジックに修正し、dev・本番の両方で実機確認済み |

状態は「未着手 / 実装中 / 検証中 / 完了」のいずれか。
「持ち越しメモ」には Stage 5 で軽微と判定した指摘を記録する。

## 並列実行メモ(handoff.md §4より)

S3・S4・S5 は S2 完了後に相互独立で並列可(S3/S4 は `src`、S5 は `.github/workflows/` と Vercel 設定のみに触るため衝突しない)。S1→S2 は直列。
