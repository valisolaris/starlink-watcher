# Sprint 進行管理

- 参照元 handoff.md: docs/spec/20260810-starlink-visibility/handoff.md
- 技術スタック判定(stack-guides.md): なし(Vite + TypeScript、UIフレームワークなし=vanilla DOM。React/Next.js不使用のため vercel-react-best-practices は非該当)
- 最終更新: 2026-08-10

## Sprint一覧

| # | 名前(1つの価値) | 依存 | 完了条件 | 状態 | 持ち越しメモ |
|---|---|---|---|---|---|
| S1 | 地点を設定・保存できるアプリの殻(デザイントークン・減光トグル込み) | なし | `npm run dev` で表示され、地点設定→リロード後も保持。減光トグルで全体輝度が下がる。prefers-reduced-motion 尊重・フォーカス可視。`npm run test`・`npm run build` exit 0。git init+初回コミット済み(コミット前にF3レビュー)。実機: モバイル幅(390px)表示確認、位置情報拒否時に手入力へ誘導されること | 完了(コミット adf5f44) | UI層DOMテスト未導入(codex軽微4、S2で導入判断)。Geolocation実ダイアログでの拒否は未検証(拒否エラー注入で同一コードパスは検証済み)。減光トグルはdesign-brief§1(ヘッダー)でなくreview.html(フッター)に準拠 |
| S2 | 設定地点の「今夜見えるか」verdictと5日分パス予報が見られる | S1 | 実在地点で5日分リストが表示され、可視判定ロジックにVitestユニット(太陽高度・照射判定・座標変換)が通る。妥当性の実機検証: heavens-above.com等の同地点予報と突き合わせ、通過時刻が±5分以内。fetch中のローディング表示あり | 未着手 | |
| S3 | 各パスの方位図で「どこを見ればいいか」が分かる | S2 | 実機で方角が実際の空と整合(北を上、東西反転していないこと)。SVG座標変換のユニットテストが通る | 未着手 | |
| S4 | 打ち上げ直後トレインが検出され優先表示される | S2 | 擬似TLEフィクスチャ(低高度群+運用高度群の混在)で検出ユニットテストが通る。トレイン不在期はバンドが出ないこと。実データ確認は当日トレインが存在する場合のみ(不在なら「フィクスチャ検証のみ・実データ未検証」と報告) | 未着手 | |
| S5 | データが毎日自動更新され、Vercelの URLでいつでも使える | S2(データ形式確定後) | workflow_dispatchの手動実行でスナップショットが生成・commitされ、Vercel本番URLでアプリが表示・予報が出る(実機確認)。スナップショットが1MB未満。cron設定がワークフローに存在する(日次実行の実観測は不要、手動実行の成功で代替可。その場合「cron実観測は未検証」と報告) | 未着手 | |

状態は「未着手 / 実装中 / 検証中 / 完了」のいずれか。
「持ち越しメモ」には Stage 5 で軽微と判定した指摘を記録する。

## 並列実行メモ(handoff.md §4より)

S3・S4・S5 は S2 完了後に相互独立で並列可(S3/S4 は `src`、S5 は `.github/workflows/` と Vercel 設定のみに触るため衝突しない)。S1→S2 は直列。
